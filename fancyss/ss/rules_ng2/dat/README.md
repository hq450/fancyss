This directory is generated by:

  scripts/build_geodata_fancyss.sh

Expected outputs:
- geosite.dat
- geoip.dat

These files are not committed by default from the current workspace because the local toolchain may not include Go.
